import { EditorDictType } from '@/types';

type Props = { t: EditorDictType };
const ChatSection = ({ t }: Props) => {
  return <div className='flex w-full flex-col overflow-y-auto'></div>;
};
export default ChatSection;
