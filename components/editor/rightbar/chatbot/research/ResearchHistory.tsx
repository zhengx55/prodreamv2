import { EditorDictType } from '@/types';
import { memo } from 'react';

type Props = { t: EditorDictType };
const ResearchHistory = (props: Props) => {
  return <div>ResearchHistory</div>;
};
export default memo(ResearchHistory);
