import { EditorDictType } from '@/types';
import { memo } from 'react';

type Props = { t: EditorDictType };
const ResearchMessageItem = ({ t }: Props) => {
  return <div>ResearchMessageItem</div>;
};
export default memo(ResearchMessageItem);
