import React from 'react';
import { Panel } from 'react-resizable-panels';

const OutputPanel = () => {
  return (
    <Panel className='flex' minSize={45} defaultSize={50}>
      OutputPanel
    </Panel>
  );
};

export default OutputPanel;
