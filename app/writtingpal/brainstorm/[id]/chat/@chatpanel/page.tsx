import React from 'react';

const ChatPanel = () => {
  return (
    <section className='relative flex h-[calc(100%_-var(--top-nav-bar-height))] w-full overflow-y-auto px-10 pt-20 md:px-0'>
      2
    </section>
  );
};

export default ChatPanel;
